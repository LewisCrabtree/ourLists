CREATE TABLE `tblLists` (
  `ListID` varchar(7) PRIMARY KEY,
  `ListName` varchar(20)
);

CREATE TABLE `tblItems` (
  `ItemID` int PRIMARY KEY AUTO_INCREMENT,
  `ItemName` varchar(30)
);

CREATE TABLE `tblListItems` (
  `ListID` varchar(7),
  `ItemID` int
);

ALTER TABLE `tblListItems` ADD FOREIGN KEY (`ListID`) REFERENCES `tblLists` (`ListID`);

ALTER TABLE `tblListItems` ADD FOREIGN KEY (`ItemID`) REFERENCES `tblItems` (`ItemID`);