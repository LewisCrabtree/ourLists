CREATE TABLE `tblLists` (
  `ListID` varchar(7) PRIMARY KEY,
  `ListName` varchar(20)
);

CREATE TABLE `tblItems` (
  `ItemID` varchar(7) PRIMARY KEY,
  `ItemName` varchar(40),
  `ListID` varchar(7)
);

ALTER TABLE `tblItems` ADD FOREIGN KEY (`ListID`) REFERENCES `tblLists` (`ListID`);